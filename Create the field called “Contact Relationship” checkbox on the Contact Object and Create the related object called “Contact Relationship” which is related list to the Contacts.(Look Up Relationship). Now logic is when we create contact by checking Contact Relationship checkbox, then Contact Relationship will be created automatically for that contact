Create the field called “Contact Relationship” checkbox on the Contact Object and Create the related object called “Contact Relationship” which is related list to the Contacts.(Look Up Relationship). Now logic is when we create contact by checking Contact Relationship checkbox, then Contact Relationship will be created automatically for that contact

Trigger :
trigger TriggerAssignment8 on Contact (after insert) 
{
    if(trigger.isAfter)
 	{
 		if(trigger.isInsert)
 		{
         TriggerAssignmentClass8 tac = New TriggerAssignmentClass8();
         tac.contactTrigger(trigger.New);
		}
 	}
}

Handler class:
public class TriggerAssignmentClass8 
{
    public void contactTrigger(list<Contact> List_Contacts)
 	{
 	  list<Contact_Relationship__c> ConList= new list<Contact_Relationship__c>();
     	for(Contact newconts:List_Contacts)
     	{
     		if(newconts.Contact_Relationship__c== true)
     		{
                Contact_Relationship__c CR = new Contact_Relationship__c();
                CR.Name=newconts.Lastname;
                CR.Contact__c= newconts.id;
                ConList.add(CR);
     		}
 		}
 	insert ConList;
    }
}
