Write a Triger to Prevent the users to create Duplicate Accounts 

Trigger :
trigger TriggerAssignment2 on Account (before insert, before update) 
{
	for(Account a:Trigger.new)
    {
        List<Account> acc=[select ID from account where Name=:a.Name];
        if(acc.size()>0)
        {
            a.adderror('You cannot create a duplicate account');
        }
    } 
    
    }
